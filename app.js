$("#card").wScratchPad({
  size: 100, // The size of the brush/scratch.
  bg: `Image/istockphoto-175413535-612x612.jpg`, // Background (image path or hex color).
  fg: `Image/front.jpg`, // Foreground (image path or hex color).
  cursor: "pointer", // Set cursor.
});
